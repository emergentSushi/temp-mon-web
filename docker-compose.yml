services:
  tempmon:
    image: tempmon
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 8080:5000
    volumes:
      - type: bind
        source: ./data
        target: /app/data
    restart: unless-stopped
